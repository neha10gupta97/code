#include<bits/stdc++.h>
using namespace std;

void allsortedarr(vector<int>&arr,int n){

}

main(){
  int n,m,i;
  cin>>n,m;
  vector<int>arr(n);
  vector<int>brr(m);
  for(i=0;i<n;i++)
    cin>>arr[i];
  for(i=0;i<m;i++)
    cin>>brr[i];
  allsortedarr(arr,n);
}
